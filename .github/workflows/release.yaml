name: Release
run-name: ${{github.workflow}} (${{github.event_name}} on ${{ github.ref_name}})

on:
  workflow_dispatch:
    inputs:
      dry-run:
        type: boolean
        default: false
        description: Do a dry-run of the release

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      GIT_AUTHOR_NAME: Joeri de Gooijer
      GIT_AUTHOR_EMAIL: joeri.degooijer@bloomreach.com.com
      GIT_COMMITER_NAME: Joeri de Gooijer
      GIT_COMMITER_EMAIL: joeri.degooijer@bloomreach.com.com
    permissions:
      contents: write # to be able to publish a GitHub release
      issues: write # to be able to comment on released issues
      pull-requests: write # to be able to comment on released pull requests
      id-token: write # to enable use of OIDC for npm provenance
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup PNPM
        uses: pnpm/action-setup@v3
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install

      - name: Lint source code
        run: pnpm lint

      - name: Build-time source code type check
        run: pnpm type-check

      - name: Run unit & integration test
        run: pnpm test-run

      - name: Build distribution assets
        run: pnpm build

      - name:
          Verify the integrity of provenance attestations and registry signatures for installed
          dependencies
        run: pnpm audit signatures

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: pnpm exec semantic-release -d ${{ github.event.inputs.dry-run }}
